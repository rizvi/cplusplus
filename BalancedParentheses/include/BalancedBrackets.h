#ifndef BALANCEDBRACKETS_H
#define BALANCEDBRACKETS_H
#include <string>

using namespace std;

bool isMatching(char opening, char closing);
bool isBalanced(string expression);
class BalancedBrackets
{
public:
    BalancedBrackets();
    virtual ~BalancedBrackets();

protected:

private:
};

#endif // BALANCEDBRACKETS_H
